// Membuat fungsi bernama greeting menggunakan arrow function
var greeting = message => {

  // Menampilkan isi dari parameter message dalam kotak alert
  alert(message);
}

// Memanggil fungsi greeting dengan mengirimkan pesan "Selamat Pagi"
greeting('Selamat Pagi');


// Membuat fungsi bernama greetingNew menggunakan arrow function
// Fungsi ini memiliki dua parameter, yaitu message1 dan message
var greetingNew = (message1, message) => {

  // Menampilkan gabungan pesan menggunakan alert
  alert(message1 + message2);
}
